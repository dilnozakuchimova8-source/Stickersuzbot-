services:
  - type: web
    name: uz-sticker-bot
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python3 bot.py
    envVars:
      - key: STICKER_BOT_TOKEN
        sync: false
    healthCheckPath: /health
